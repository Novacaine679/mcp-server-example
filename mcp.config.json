{
  "version": "0.4.0",
  "server": {
    "host": "localhost",
    "port": 3000,
    "basePath": "/api/mcp"
  },
  "plugins": [
    {
      "name": "simple-plugin",
      "enabled": true,
      "config": {
        "modelName": "simple-model",
        "maxTokens": 1000,
        "temperature": 0.7
      }
    }
  ],
  "logging": {
    "level": "info",
    "format": "json"
  }
}